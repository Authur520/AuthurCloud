spring:
  application:
    name: Authur-Server-System
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8008
      config:
        server-addr: ${nacos.url}:8008
        group: DEFAULT_GROUP
        prefix: authur-server-system
        file-extension: yaml
logging:
  level:
    com.alibaba.nacos.client: error