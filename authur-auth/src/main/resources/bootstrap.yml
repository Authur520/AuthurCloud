spring:
  application:
    name: Authur-Auth
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8008
      config:
        server-addr: ${nacos.url}:8008
        group: DEFAULT_GROUP
        prefix: authur-auth
        file-extension: yaml